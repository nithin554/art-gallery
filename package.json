{
  "name": "art-gallery",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:logos": "node scripts/generate-logos.mjs",
    "prebuild": "npm run build:logos",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.1.5"
  }
}
