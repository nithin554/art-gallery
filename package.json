{
  "name": "art-gallery",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:logos": "node scripts/generate-logos.mjs",
    "prebuild": "npm run build:logos",
    "build": "vite build",
    "preview": "vite preview",
    "worker:dev": "wrangler dev --config worker/wrangler.toml",
    "worker:deploy": "wrangler deploy --config worker/wrangler.toml"
  },
  "devDependencies": {
    "vite": "^8.2.0",
    "wrangler": "^4.120.0"
  }
}
